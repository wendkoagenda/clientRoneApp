/**
 * Enum for work order status.
 * @readonly
 * @enum {{name: string, position: string}}
 */
const badgePosition = {
    LEFT_SIDEBAR: 'Left Sidebar',
    TIMELINE: 'Timeline'
};

export default badgePosition;